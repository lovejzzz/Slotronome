{
  "name": "slotronome",
  "version": "1.0.0",
  "description": "A pixel-art slot machine that happens to be a metronome",
  "private": true,
  "scripts": {
    "test": "node test/run.js",
    "test:all": "node test/run.js --slow",
    "test:headed": "node test/run.js --headed",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  },
  "comment": "devDependencies are for the test runner only. The app itself is plain HTML, CSS and JavaScript with no runtime dependencies and no build step — index.html opens straight off disk."
}
